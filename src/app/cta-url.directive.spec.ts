import { CtaUrlDirective } from './cta-url.directive';

describe('CtaUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new CtaUrlDirective();
    expect(directive).toBeTruthy();
  });
});
