import { HTMlDirective } from './shared/directives/html.directive';

describe('HTMlDirective', () => {
  it('should create an instance', () => {
    const directive = new HTMlDirective();
    expect(directive).toBeTruthy();
  });
});
